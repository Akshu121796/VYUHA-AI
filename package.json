{
    "name": "vyuha-ai",
    "private": true,
    "scripts": {
        "frontend": "cd frontend && npm run dev",
        "backend": "cd backend && npm run dev",
        "dev": "concurrently -n FRONTEND,BACKEND -c blue,green \"npm run backend\" \"npm run frontend\"",
        "build": "(pip3 install supabase python-dotenv requests || pip install supabase python-dotenv requests || echo 'pip install skipped') && npm run build --prefix frontend && npm run build --prefix backend"
    },
    "devDependencies": {
        "concurrently": "^9.0.0"
    }
}