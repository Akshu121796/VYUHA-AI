{
    "name": "vyuha-ai",
    "private": true,
    "scripts": {
        "frontend": "cd frontend && npm run dev",
        "backend": "cd backend && npm run dev",
        "dev": "concurrently -n FRONTEND,BACKEND -c blue,green \"npm run backend\" \"npm run frontend\""
    },
    "devDependencies": {
        "concurrently": "^9.0.0"
    }
}